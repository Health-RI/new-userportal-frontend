server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    add_header Content-Security-Policy "script-src 'self' 'unsafe-inline';style-src 'self' 'unsafe-inline';img-src 'self' data:;font-src 'self' https://fonts.gstatic.com;frame-ancestors 'self' ${IDENTITY_SERVER_HOST_URL};block-all-mixed-content";

    location / {
        try_files $uri $uri/ /index.html;
    }
}
